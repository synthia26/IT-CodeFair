import React from 'react';
import ReactDOM from 'react-dom';
import HouseForm from './HouseForm';

// Render the React component into a DOM element with id 'react-house-form'
ReactDOM.render(<HouseForm />, document.getElementById('react-house-form'));